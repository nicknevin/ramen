apiVersion: apps/v1
kind: Deployment
metadata:
  name: csi-cephfsplugin-provisioner
  namespace: rook-ceph
spec:
  template:
    spec:
      containers:
      - name: csi-addons
        args:
        - --listen-port=9070
        - --log-level=5
        - --enable-auth=false