apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: csi-cephfsplugin
  namespace: rook-ceph
spec:
  template:
    spec:
      containers:
      - name: csi-addons
        args:
        - --listen-port=9070
        - --log-level=5
        - --enable-auth=false