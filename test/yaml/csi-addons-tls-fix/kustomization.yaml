apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []

patchesStrategicMerge:
  - rbd-provisioner-patch.yaml
  - cephfs-provisioner-patch.yaml
  - rbd-plugin-patch.yaml
  - cephfs-plugin-patch.yaml